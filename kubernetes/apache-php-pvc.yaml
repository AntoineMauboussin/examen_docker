apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: www-data
  namespace: amaubou
spec:
  storageClassName: nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
